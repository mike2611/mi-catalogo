import { RangoModelosDirective } from './rango-modelos.directive';

describe('RangoModelosDirective', () => {
  it('should create an instance', () => {
    const directive = new RangoModelosDirective();
    expect(directive).toBeTruthy();
  });
});
